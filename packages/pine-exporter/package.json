{
  "name": "@pinescript-utils/pine-exporter",
  "version": "1.0.0",
  "description": "Generate valid PineScript v5 code from internal strategy definitions",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@pinescript-utils/core": "file:../core"
  },
  "devDependencies": {
    "typescript": "^5.3.2"
  }
}
